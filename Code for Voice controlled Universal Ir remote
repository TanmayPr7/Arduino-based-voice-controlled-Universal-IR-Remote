# This the source caode for Arduino based Voice controlled IR Universal remote controll project

#include <IRremote.h>

IRsend irsend;
int led = 13;
String voice;

void setup(){
  Serial.begin(9600);
  pinMode(led, OUTPUT);
}
